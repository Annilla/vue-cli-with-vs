@using Settings = Project.Settings

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - 我的 ASP.NET 應用程式</title>
    @if (Settings.UseWebpackDevServer)
    {
        <link rel="stylesheet" href="@(Settings.WebpackDevServerRoot)/Content/app.build.css">
    } else {
        <link rel="stylesheet" href="/Content/app.build.css">
    }
</head>
<body>
    <div id="app"></div>
    @RenderBody()

    @if (Settings.UseWebpackDevServer)
    {
        <script src="@(Settings.WebpackDevServerRoot)/Scripts/app.build.js"></script>
    } else {
        <script src="/Scripts/app.build.js"></script>
    }
    @RenderSection("scripts", required: false) 
</body>
</html>
